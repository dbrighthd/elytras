{
  "context": "equippable",
  "items": [
    "minecraft:elytra"
  ],
  "modelPrefix": "elytras/mojang_elytras/home/",
  "type": "component_data",
  "parameters": {
    "componentType": "custom_name",
    "expect": "rich_text",
    "transform": [
      {
        "function": "alternative",
        "alternatives": [
          {
            "regex": "(?i).*home(?!( but good)) elytra.*",
            "substitution": "elytra",
            "matchAll": true
          },
          {
            "regex": "(?i).*home(?!( but good)) cape.*",
            "substitution": "elytra",
            "matchAll": true
          }
        ]
      },
      "sanitize_path"
    ]
  }
}
